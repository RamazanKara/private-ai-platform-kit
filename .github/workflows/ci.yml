name: CI

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read
  packages: write
  id-token: write
  security-events: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: azure/setup-helm@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - name: Install validation tools
        run: |
          set -euo pipefail
          scripts/install-validation-tools.sh --bin-dir "$HOME/.local/bin"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Validate
        run: make validate-full

  image:
    runs-on: ubuntu-latest
    needs: validate
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push gateway image
        uses: docker/build-push-action@v6
        with:
          context: services/inference-gateway
          push: true
          tags: ghcr.io/${{ github.repository }}/inference-gateway:${{ github.sha }}
      - name: Build and push RAG service image
        uses: docker/build-push-action@v6
        with:
          context: services/rag-service
          push: true
          tags: ghcr.io/${{ github.repository }}/rag-service:${{ github.sha }}
      - uses: anchore/sbom-action@v0
        with:
          image: ghcr.io/${{ github.repository }}/inference-gateway:${{ github.sha }}
          format: spdx-json
          output-file: inference-gateway.spdx.json
      - uses: anchore/sbom-action@v0
        with:
          image: ghcr.io/${{ github.repository }}/rag-service:${{ github.sha }}
          format: spdx-json
          output-file: rag-service.spdx.json
      - uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ghcr.io/${{ github.repository }}/inference-gateway:${{ github.sha }}
          format: sarif
          output: trivy-results.sarif
      - uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ghcr.io/${{ github.repository }}/rag-service:${{ github.sha }}
          format: sarif
          output: trivy-rag-results.sarif
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-rag-results.sarif
      - uses: sigstore/cosign-installer@v3
      - name: Sign image
        run: |
          cosign sign --yes ghcr.io/${{ github.repository }}/inference-gateway:${{ github.sha }}
          cosign sign --yes ghcr.io/${{ github.repository }}/rag-service:${{ github.sha }}
