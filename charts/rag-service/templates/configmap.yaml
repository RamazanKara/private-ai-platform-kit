apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rag-service.fullname" . }}-knowledge
  labels:
    {{- include "rag-service.labels" . | nindent 4 }}
data:
{{- range $name, $body := .Values.knowledge.documents }}
  {{ $name | quote }}: |
{{ $body | nindent 4 }}
{{- end }}
