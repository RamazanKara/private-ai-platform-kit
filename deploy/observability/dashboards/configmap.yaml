# Generated by scripts/dashboard-check.py --write. Do not edit by hand.
---
apiVersion: v1
data:
  inference-dashboard.json: |
    {
      "annotations": {"list": []},
      "editable": true,
      "panels": [
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "targets": [
            {
              "expr": "sum(rate(inference_gateway_requests_total[5m])) by (backend, status)",
              "legendFormat": "{{backend}} {{status}}"
            }
          ],
          "title": "Gateway requests",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(inference_gateway_request_duration_seconds_bucket[5m])) by (le, backend))",
              "legendFormat": "p95 {{backend}}"
            }
          ],
          "title": "Gateway p95 latency",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
          "id": 3,
          "targets": [
            {
              "expr": "sum(rate(inference_gateway_sandbox_requests_total[5m])) by (sandbox, backend, status)",
              "legendFormat": "{{sandbox}} {{backend}} {{status}}"
            }
          ],
          "title": "Sandbox request rate",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
          "id": 4,
          "targets": [
            {
              "expr": "sum(rate(inference_gateway_tokens_total[5m])) by (backend, token_type)",
              "legendFormat": "{{backend}} {{token_type}}"
            }
          ],
          "title": "Runtime token usage",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16},
          "id": 5,
          "targets": [
            {
              "expr": "sum(rate(inference_gateway_admission_rejections_total[5m])) by (reason, sandbox)",
              "legendFormat": "{{sandbox}} {{reason}}"
            }
          ],
          "title": "Admission rejections",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16},
          "id": 6,
          "targets": [
            {
              "expr": "inference_gateway_sandbox_budget_usage / clamp_min(inference_gateway_sandbox_budget_limit, 1)",
              "legendFormat": "{{sandbox}} {{budget_type}}"
            }
          ],
          "title": "Sandbox budget utilization",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "currencyUSD"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 24},
          "id": 7,
          "targets": [
            {
              "expr": "sum(rate(inference_gateway_estimated_cost_usd_total[5m])) by (sandbox)",
              "legendFormat": "{{sandbox}}"
            }
          ],
          "title": "Estimated cost rate by sandbox (USD/s)",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "currencyUSD"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 24},
          "id": 8,
          "targets": [
            {
              "expr": "sum(increase(inference_gateway_estimated_cost_usd_total[24h])) by (sandbox)",
              "legendFormat": "{{sandbox}}"
            }
          ],
          "title": "Estimated cost by sandbox (last 24h)",
          "type": "timeseries"
        }
      ],
      "schemaVersion": 39,
      "tags": ["private-ai-platform-kit"],
      "title": "Private AI Platform Kit Inference",
      "uid": "ai-platform-inference",
      "version": 1
    }
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: private-ai-platform-kit
    grafana_dashboard: '1'
  name: grafana-dashboard-inference-dashboard
  namespace: monitoring
---
apiVersion: v1
data:
  opencost-dashboard.json: |
    {
      "annotations": {"list": []},
      "editable": true,
      "panels": [
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "currencyUSD"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "targets": [
            {
              "expr": "sum(node_total_hourly_cost)",
              "legendFormat": "cluster $/hour"
            }
          ],
          "title": "Cluster node cost (USD/hour)",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "targets": [
            {
              "expr": "sum(container_cpu_allocation) by (namespace)",
              "legendFormat": "{{namespace}}"
            }
          ],
          "title": "CPU allocation by namespace (OpenCost)",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "bytes"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
          "id": 3,
          "targets": [
            {
              "expr": "sum(container_memory_allocation_bytes) by (namespace)",
              "legendFormat": "{{namespace}}"
            }
          ],
          "title": "Memory allocation by namespace (OpenCost)",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
          "id": 4,
          "targets": [
            {
              "expr": "sum(container_gpu_allocation) by (namespace)",
              "legendFormat": "{{namespace}}"
            }
          ],
          "title": "GPU allocation by namespace (OpenCost)",
          "type": "timeseries"
        }
      ],
      "schemaVersion": 39,
      "tags": ["private-ai-platform-kit", "cost"],
      "title": "Private AI Platform Kit Cost & Allocation",
      "uid": "ai-platform-cost",
      "version": 1
    }
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: private-ai-platform-kit
    grafana_dashboard: '1'
  name: grafana-dashboard-opencost-dashboard
  namespace: monitoring
---
apiVersion: v1
data:
  rag-dashboard.json: |
    {
      "annotations": {"list": []},
      "editable": true,
      "panels": [
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "targets": [
            {
              "expr": "sum(rate(rag_service_requests_total[5m])) by (route, status)",
              "legendFormat": "{{route}} {{status}}"
            }
          ],
          "title": "RAG requests",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(rag_service_request_duration_seconds_bucket[5m])) by (le, route))",
              "legendFormat": "p95 {{route}}"
            }
          ],
          "title": "RAG p95 latency",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
          "id": 3,
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(rag_service_retrieval_results_bucket[5m])) by (le, sandbox))",
              "legendFormat": "p95 {{sandbox}}"
            }
          ],
          "title": "Retrieved documents per query",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
          "id": 4,
          "targets": [
            {
              "expr": "sum(rate(rag_service_auth_failures_total[5m])) by (route, reason)",
              "legendFormat": "{{route}} {{reason}}"
            }
          ],
          "title": "RAG authentication failures",
          "type": "timeseries"
        }
      ],
      "schemaVersion": 39,
      "tags": ["private-ai-platform-kit"],
      "title": "Private AI Platform Kit RAG",
      "uid": "ai-platform-rag",
      "version": 1
    }
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: private-ai-platform-kit
    grafana_dashboard: '1'
  name: grafana-dashboard-rag-dashboard
  namespace: monitoring
---
apiVersion: v1
data:
  restore-drill-dashboard.json: |+
    {
      "annotations": {"list": []},
      "editable": true,
      "panels": [
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "targets": [
            {
              "expr": "restore_drill_validation_passed",
              "legendFormat": "validation passed"
            }
          ],
          "title": "Restore drill validation",
          "type": "stat"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "targets": [
            {
              "expr": "restore_drill_backup_age_seconds",
              "legendFormat": "backup age"
            }
          ],
          "title": "Backup age seconds",
          "type": "timeseries"
        }
      ],
      "schemaVersion": 39,
      "tags": ["private-ai-platform-kit", "restore-drill"],
      "title": "Private AI Platform Kit Restore Drill",
      "uid": "ai-platform-restore-drill",
      "version": 1
    }

kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: private-ai-platform-kit
    grafana_dashboard: '1'
  name: grafana-dashboard-restore-drill-dashboard
  namespace: monitoring
---
apiVersion: v1
data:
  vllm-gpu-dashboard.json: |
    {
      "annotations": {"list": []},
      "editable": true,
      "panels": [
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "percent"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "targets": [
            {
              "expr": "DCGM_FI_DEV_GPU_UTIL",
              "legendFormat": "gpu {{gpu}} {{Hostname}}"
            }
          ],
          "title": "GPU utilization",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "decmbytes"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "targets": [
            {
              "expr": "DCGM_FI_DEV_FB_USED",
              "legendFormat": "used gpu {{gpu}} {{Hostname}}"
            },
            {
              "expr": "DCGM_FI_DEV_FB_FREE",
              "legendFormat": "free gpu {{gpu}} {{Hostname}}"
            }
          ],
          "title": "GPU VRAM used and free",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "celsius"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
          "id": 3,
          "targets": [
            {
              "expr": "DCGM_FI_DEV_GPU_TEMP",
              "legendFormat": "gpu {{gpu}} {{Hostname}}"
            }
          ],
          "title": "GPU temperature",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "watt"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
          "id": 4,
          "targets": [
            {
              "expr": "DCGM_FI_DEV_POWER_USAGE",
              "legendFormat": "gpu {{gpu}} {{Hostname}}"
            }
          ],
          "title": "GPU power usage",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16},
          "id": 5,
          "targets": [
            {
              "expr": "sum(vllm:num_requests_running) by (model_name)",
              "legendFormat": "running {{model_name}}"
            },
            {
              "expr": "sum(vllm:num_requests_waiting) by (model_name)",
              "legendFormat": "waiting {{model_name}}"
            }
          ],
          "title": "vLLM requests running and waiting",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "percentunit"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16},
          "id": 6,
          "targets": [
            {
              "expr": "sum(vllm:gpu_cache_usage_perc) by (model_name)",
              "legendFormat": "kv-cache {{model_name}}"
            }
          ],
          "title": "vLLM KV-cache usage",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 24},
          "id": 7,
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(vllm:time_to_first_token_seconds_bucket[5m])) by (le, model_name))",
              "legendFormat": "p95 ttft {{model_name}}"
            }
          ],
          "title": "vLLM time to first token p95",
          "type": "timeseries"
        },
        {
          "datasource": "Prometheus",
          "fieldConfig": {"defaults": {"unit": "tps"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 24},
          "id": 8,
          "targets": [
            {
              "expr": "sum(rate(vllm:generation_tokens_total[5m])) by (model_name)",
              "legendFormat": "generation tokens/s {{model_name}}"
            }
          ],
          "title": "vLLM generation throughput",
          "type": "timeseries"
        }
      ],
      "schemaVersion": 39,
      "tags": ["private-ai-platform-kit", "vllm", "gpu"],
      "title": "Private AI Platform Kit vLLM and GPU",
      "uid": "ai-platform-vllm-gpu",
      "version": 1
    }
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/part-of: private-ai-platform-kit
    grafana_dashboard: '1'
  name: grafana-dashboard-vllm-gpu-dashboard
  namespace: monitoring
