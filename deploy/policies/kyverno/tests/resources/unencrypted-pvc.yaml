apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unencrypted-pvc
  namespace: vllm
  labels:
    app.kubernetes.io/name: vllm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
