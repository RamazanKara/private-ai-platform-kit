apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: unscoped-egress
  namespace: tenant-a
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - ports:
        - protocol: TCP
          port: 443
