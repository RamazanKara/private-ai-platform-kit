{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "vllm values",
  "type": "object",
  "properties": {
    "replicaCount": { "type": "integer", "minimum": 0 },
    "model": {
      "type": "object",
      "properties": {
        "task": { "type": "string", "enum": ["generate", "embed", "classify", "score", "reward"] }
      }
    },
    "accelerator": {
      "type": "object",
      "properties": {
        "vendor": { "type": "string", "enum": ["nvidia", "amd"] },
        "count": { "type": "integer", "minimum": 1 }
      }
    }
  }
}
