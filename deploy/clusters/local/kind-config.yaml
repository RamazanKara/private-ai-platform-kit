kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: private-ai-platform-kit
nodes:
  - role: control-plane
    image: kindest/node:v1.35.1
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true,platform.ai/node-pool=local"
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
