namespace:
  name: ai-agents

sandbox:
  id: agent-lab
  tenant: coding-agents
  owner: agent-platform
  costCenter: research
  environment: local
  # Flip to agent-sandbox after `make agent-sandbox-install` (ADR 0009).
  runtime: namespace

workspace:
  pvc:
    size: 5Gi
